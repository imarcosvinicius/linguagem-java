package sintaxeBasica;

public class AreaTrapezio {
    public static void main(String[] args) {

        double topo, base, altura, area;

        topo = 6.0;
        base = 8.0;
        altura = 5.0;

        area = (topo + base) / 2.0 * altura;
        System.out.println(area);

    }
}